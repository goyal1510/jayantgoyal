# Platform restructure baseline

- Date: 2026-07-17
- Area: Four-application platform restructure
- Problem: The current combined application and Admin deployment need a verified migration path to Portfolio, Studio, Admin, and Auth without disrupting production behavior.
- Status: PLATFORM-00 remains open for its final proof gates and the app-first split is in progress. The application moved directly from `apps/jayantgoyal`/package `jg` to `apps/studio`/package `studio`; the existing Vercel project is now `jayantgoyal-studio` rooted at `apps/studio`; and the apex/`www` domains now belong to Portfolio. The shared branding/metadata contract is implemented, and Portfolio now uses the same expandable/collapsible sidebar structure as Studio while owning its implementation locally; exact-commit deployments and post-cutover validation remain pending.
- Decisions: Portfolio, Studio, and Admin folder/package/filter identities must match exactly (`portfolio`, `studio`, `admin`). The application names are `Portfolio`, `Studio`, and `Admin`; the searchable public identity is `Jayant Goyal`; and the app-level metadata identities are `Jayant Goyal`, `Studio by Jayant Goyal`, and `Admin by Jayant Goyal`. Repeated page titles stay functional and concise: Portfolio pages use `%s | Jayant Goyal`, Studio pages use `%s | Studio`, and Admin pages use `%s | Admin`; ownership wording is not repeated on pages such as Users, Deployments, Hero, or Weather. `JG` is reserved for visual or legacy technical identifiers, not written application branding. These values now live in a dependency-free `@repo/brand` package consumed by every app. Sidebar composition is deliberately app-local: Studio retains its established implementation, Admin retains the same structural pattern with role-gated sections, and Portfolio replicates the same `Sidebar`/header/content/footer/rail behavior with Portfolio-owned navigation. Portfolio's top header also follows Studio's sidebar-aware height transition and dynamic-breadcrumb structure. No centralized application-sidebar shell is used. Studio is a single-purpose application on every host rather than a dual Portfolio/Studio surface; Portfolio-owned legacy paths redirect to the canonical Portfolio origin. Complete shared Auth/SSO last. Use one program PR with reviewable commits and preserve e-commerce projects outside the migration.
- Evidence: The canonical Supabase project, current Vercel projects, current Cloudflare records, and hosted Auth URL/provider settings were read without exposing secrets. The scoped Management API patch updated `site_url` and `uri_allow_list`; the recovery template was then corrected from `{{ .SiteURL }}` to `{{ .RedirectTo }}` so it honors the application's exact callback. Read-after-write checks confirmed the values and that Google/email remain enabled. A non-user hosted recovery request accepted the production callback with HTTP `200`. Vercel read-back checks confirmed exact Development, Production, `staging`, and generic Preview URL contracts across Portfolio, Studio, and Admin. The existing product project was renamed/repointed in place and its stale Portfolio/Resume/Commerce environment variables were removed; Admin's project key now says Studio. The persistent remote `staging` branch was created from the verified `origin/main` baseline. Portfolio and Studio pass focused TypeScript, zero-warning ESLint, and production builds. Local browser validation confirmed the complete Portfolio, mobile Portfolio navigation, Studio inventory, product-link handoff, and no captured console errors. Cloudflare and Vercel confirm exact DNS for the subdomains. Vercel moved the apex and `www` to Portfolio, made the apex canonical, and assigned the tested Portfolio deployment. Live checks confirm Portfolio root/content surfaces, single-hop professional aliases, method-preserving product/Auth handoffs to Studio, the `www` redirect, and Studio availability. The repository-local Vitest suite now passes six files and sixteen tests without credentials or provider calls, including five brand-contract tests that lock the full `Jayant Goyal` app-level identity, the `Portfolio`/`Studio`/`Admin` application names, concise page-title composition, and the prohibition on `JG` or repeated ownership wording as written page branding. Studio and Admin were restored exactly through Git revert before the app-local Portfolio sidebar was added. Full monorepo type checking, zero-warning lint, and production builds pass. Rendered browser-title checks pass for `Jayant Goyal | Full-Stack Developer`, `Blog | Jayant Goyal`, `Studio by Jayant Goyal | Apps, Tools, and Experiments`, `Weather | Studio`, `UUID Generator | Studio`, `Hero | Admin`, `Users | Admin`, and `Deployments | Admin`. Local Portfolio browser checks pass for expanded structure, icon collapse, section scrolling, active state, mobile drawer, Blog navigation/title, automatic mobile close, and zero captured console errors. The Studio-style Portfolio header was measured at `64px` expanded and `48px` collapsed; Blog and article breadcrumbs update correctly and the final header check captured no console errors. After the final targeted dependency remediation, the production-dependency audit also passes; `pnpm audit --prod` reports no known vulnerabilities.
- Files changed: New independent `apps/portfolio` application and public-only integration boundary; public Blog, Resume, contact Server Action and compatibility API, sitemap, `llms.txt`, not-found, and error surfaces; app-local Portfolio sidebar/header/footer/rail composition matching Studio's expand/collapse and mobile structure while preserving Portfolio section, Blog, Resume, theme, and Studio navigation; app-local Studio and Admin sidebar implementations; canonical app-level branding plus concise cross-application title templates; explicit Admin metadata for content, user, deployment, MFA, welcome, and access-denied surfaces; explicit Studio MFA and loader-preview metadata; branded page-level Open Graph/Twitter titles, including Portfolio articles; Portfolio-to-Studio compatibility redirects and redirect ledger; corrected Portfolio/Studio Vercel ownership, Development-port contract, and cross-app Portfolio/self URL contracts; physical rename to `apps/studio` with package/filter `studio`; removal of dual-host Studio identity selection; Studio-only root, navigation, metadata, manifest, robots, sitemap, structured data, and local-port behavior; dependency-free `@repo/brand` constants and contract tests adopted by Portfolio, Studio, and Admin metadata, manifests, and Studio interface branding; focused Vitest coverage for branding, hostname normalization, URL resolution, redirect ownership, and contact validation; patched Next.js and safe transitive dependency resolutions; repository-local Vercel and Cloudflare tooling; PDF binary Git handling for copied portfolio credentials; platform URL helpers; platform decision log; proof ledger; implementation-guide phase status/checklists/order; repository environment task inputs; and this session entry.
- Risks: Current OAuth uses a `next` query parameter in `redirectTo`, so the temporary same-origin wildcard callback paths remain until the code moves the validated destination into an httpOnly handoff cookie. Vercel's free-team daily limit blocks fresh Portfolio/Studio builds, so tested immutable previews temporarily back the custom domains. The current Portfolio deployment still emits the pre-cutover Portfolio subdomain as canonical/Open Graph URL; the remote Production URL is corrected, but a fresh deployment is required. Deployed Chrome validation is pending because the extension connection is unavailable even though Chrome, the extension, and the native-host configuration are present. The development audit retains six high findings only inside current Vercel CLI's upstream major-5 `undici`; the production dependency graph is clean. GitHub CLI authentication was renewed for the required personal `goyal1510` account, and repository-local SSH routing and author identity were reverified. Do not run an unrestricted `supabase config push` from the normal worktree.
