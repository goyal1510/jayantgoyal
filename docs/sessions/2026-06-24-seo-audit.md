# SEO Audit Implementation

Date: 2026-06-24
App/area: apps/jayantgoyal SEO metadata, crawlability, and structured data

## Problem

Run an SEO audit pass for the portfolio/productivity hub and implement high-confidence fixes found in the codebase.

## Progress

- Created worktree from fresh origin/main because the source clone is protected.
- Copied local env files into the worktree without exposing values.
- Started audit from the repository rules and attached SEO rubric.
- Found public SEO drift: sitemap manually duplicated tool paths, `/blogs` was conditional on Supabase availability, broad public route matching could classify sibling paths as public, and profile/software JSON-LD was emitted on every route.
- Added shared SEO config for canonical URL constants, public/indexable route matching, metadata generation, and stable significant-update timestamps.
- Scoped Person/ProfilePage JSON-LD to the homepage and SoftwareApplication JSON-LD to `/tools`.
- Updated sitemap to derive tool URLs from the live `allTools` registry, always include `/blogs`, and use the shared canonical site URL/update date.
- Tightened public route matching in the proxy and auth gate to exact path-or-child semantics instead of broad prefix matching.
- Added canonical/Open Graph/Twitter metadata to the main public utility pages and blog posts.

## Validation

- `pnpm --filter jg lint` passed.
- `pnpm --filter jg check-types` passed.
- `pnpm --filter jg build` passed.
- `git diff --check` passed.

## Continued SEO Goal Work

- Started branded-search cleanup to make the homepage and structured data consistently identify the profile as `Jayant Goyal` instead of only `Jayant`.
- Updated shared SEO constants, homepage metadata, Person/ProfilePage/author JSON-LD, and fallback portfolio data to use the full name.
- Aligned root metadata authorship and web manifest description with the full-name branding signal.
- Updated remote Supabase `portfolio.hero.name` from `JAYANT` to `Jayant Goyal` and the `portfolio.about.personal` Name value from `Jayant` to `Jayant Goyal`, then verified both rows.

## Continued Validation

- `pnpm --filter jg lint` passed after full-name branding changes.
- `pnpm --filter jg check-types` passed after full-name branding changes.
- `pnpm --filter jg build` passed after full-name branding changes.

## Priority Tool Content Expansion

- Added server-rendered reference content for high-value tool pages: UUID Generator, Random Port Generator, Crontab Generator, JSON Prettify, JSON Minify, Regex Tester, Base64 Encoder/Decoder, WiFi QR Code Generator, Hash Text, and JWT Parser.
- Added FAQPage JSON-LD plus per-tool SoftwareApplication JSON-LD for those priority tool pages.
- Wired the content through the shared tools layout so the priority pages get richer crawlable content without duplicating page components.
- `git diff --check` passed after the priority tool content changes.
- `pnpm --filter jg lint` passed after the priority tool content changes.
- `pnpm --filter jg check-types` passed after the priority tool content changes.
- `pnpm --filter jg build` passed after the priority tool content changes.

## Branded About Route

- Added `/about` as a public, indexable profile page for "Jayant Goyal full-stack developer" queries.
- Reused portfolio data for the About page so the route stays aligned with database-backed homepage content.
- Added ProfilePage and breadcrumb JSON-LD, sitemap coverage, public route access, and sidebar navigation for `/about`.
- `git diff --check` passed after the branded About route changes.
- `pnpm --filter jg lint` passed after the branded About route changes.
- `pnpm --filter jg check-types` passed after the branded About route changes.
- `pnpm --filter jg build` passed after the branded About route changes.

## Blog Internal Links And Search Cleanup

- Added "Related resources" sections to the three published Supabase blog posts so crawlers and readers have direct internal links to `/tools`, `/about`, `/blogs`, `/github-stats`, and the homepage.
- Verified the published blog content through the Supabase REST API and confirmed the rendered live blog page includes the new internal links.
- Verified the `/about` production deployment became live, the live sitemap includes `/about`, and a priority tool page renders FAQPage and SoftwareApplication JSON-LD in production.
- Added `X-Robots-Tag: noindex, noarchive` for the legacy static resume PDF at `/assets/Jayant_Resume.pdf` after Google search showed that stale PDF result.
- `git diff --check` passed after the blog-link/search-cleanup changes.
- `pnpm --filter jg lint` passed after the blog-link/search-cleanup changes.
- `pnpm --filter jg build` passed after the blog-link/search-cleanup changes.
- `pnpm --filter jg check-types` passed when rerun sequentially after the build regenerated `.next/types`.
- Verified a cache-busted production request for `/assets/Jayant_Resume.pdf` returns `X-Robots-Tag: noindex, noarchive`.
- Purged Vercel CDN cache from a temporary linked directory, but the exact legacy PDF URL was still served from Cloudflare cache without the new header; resolving that exact URL requires a Cloudflare cache purge or waiting for the cached object to expire.

## Admin Subdomain Deindexing

- Google search results showed `admin.jayantgoyal.com` indexed as "Admin Panel".
- Added site-wide `robots` metadata and an `X-Robots-Tag: noindex, nofollow, noarchive` response header to the admin app so the admin subdomain can be removed from search results after recrawl.
- `git diff --check` passed after the admin deindexing changes.
- `pnpm --filter admin lint` passed after the admin deindexing changes.
- `pnpm --filter admin check-types` passed after the admin deindexing changes.
- `pnpm --filter admin build` passed after the admin deindexing changes.
