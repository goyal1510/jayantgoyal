# 2026-04-26 — SEO Files for Main App

## Area
`apps/jayantgoyal` — SEO and discoverability

## Problem
Site lacked robots.txt, sitemap.xml, web app manifest, and structured data (JSON-LD) for search engines.

## Solution
Created 4 files in `apps/jayantgoyal/src/`:

- `app/robots.ts` — robots.txt with disallow rules for auth/API routes
- `app/sitemap.ts` — full sitemap covering public pages, all tools, and games
- `app/manifest.ts` — PWA manifest with app name, icons, theme color
- `components/seo/json-ld.tsx` — Person and WebSite structured data components

All use Next.js Metadata API (typed exports). JSON-LD components are server-compatible and ready to be added to the root layout.

## Metadata Update Pass

Updated metadata across 16 files to use short titles (template appends "| Jayant Goyal") and improved descriptions:

- `manifest.ts` — updated name, short_name, description
- 13 page files — removed "| Jayant" suffix from titles, updated descriptions to be more descriptive
- 3 pages already had metadata but needed title/description improvements (tracker, management, calculator history)
