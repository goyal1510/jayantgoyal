# Post-merge shared foundation

- Date: 2026-07-17
- Area: Shared UI infrastructure and post-merge platform evidence
- Problem: The application split is merged and deployed, but the program records still contain pre-merge risks, sidebar preference parsing is repeated across all three applications, route-loading behavior differs between Studio and Admin, and shared UI mechanics lack focused automated coverage.
- Planned solution: Reconcile the proof/status records with merge commit `4703e7c`, extract only behavior-neutral sidebar preference and route-loading mechanics into `@repo/ui`, preserve app-owned navigation/auth policy, add focused tests, and validate Portfolio, Studio, and Admin before beginning Admin information architecture work.
- Decisions: Authentication sessions, Supabase clients, callbacks, MFA, roles, and redirects remain application-owned. No new shared package will be introduced for these small UI mechanics. PR #39 is preserved as the merged application-split boundary; ADR-005 records focused PRs for subsequent work.
- Evidence: The worktree starts exactly at merged commit `4703e7c`; the canonical Supabase link was verified without exposing values and its ephemeral pooler file was removed. Production roots for Portfolio and Studio return `200`; Admin performs the expected one-hop redirect and returns `200`. Portfolio and Studio staging return `200`, while Admin staging DNS is currently missing.
- Files changed: Added server-safe sidebar preference parsing and route-navigation intent helpers to `@repo/ui`; made the shared sidebar consume the same constants/clamping contract; moved Studio's non-destructive route-loading overlay into `@repo/ui`; switched Admin and Studio to the shared provider; switched all three apps to the shared sidebar cookie parser; removed the two duplicated app-local route providers; added focused Vitest coverage for cookie restoration and navigation intent; and centralized Admin's app-owned navigation inventory/route mapping into the approved Portfolio, Studio, and System domains with role-aware tests. Portfolio includes Blog; System contains Users and Deployments. Studio remains an explicit empty domain in the model and is not rendered until a real, separately approved product-catalog contract exists. The legacy environment-manager route keeps its System breadcrumb for compatibility but is no longer advertised in navigation because the blueprint forbids Admin from becoming a browser-based secrets manager. No placeholder routes, database changes, or cross-application implementation imports were introduced.
- Verification: `pnpm test` passes twelve files and forty-nine tests. Full monorepo TypeScript, zero-warning lint, and Portfolio/Studio/Admin production builds pass. After the final Admin architecture correction, focused Admin and `@repo/ui` TypeScript/lint, the complete test suite, and the Admin production build pass again. Authenticated local browser checks confirmed route rendering, active navigation/breadcrumb updates, and collapse persistence across reload at a `48px` header. One Next.js smooth-scroll development warning and existing Supabase session warnings remain; no new browser failure was found.
- Security review: No environment values, credentials, tokens, cookies, personal records, or provider responses are tracked. No database or Supabase migration was attempted. The legacy environment-manager link was removed from navigation; the route itself remains a recorded Phase 10 security/compatibility gap. Auth clients, roles, sessions, callbacks, and service-role boundaries were not changed.
- Status: Deployment records reconciled, shared UI implementation and Admin's current-route organization complete, and local verification passed. Ready for the focused post-merge PR; PLATFORM-10 remains open for the approved Studio catalog and security gates.
- Reviewability: Restored the shared sidebar's existing file style after the targeted formatter exposed unrelated whole-file churn; the final diff contains only the cookie/width contract changes.
- Cleanup: The empty Studio Admin domain uses no route or fallback URL; its navigation will be activated only with the approved catalog surface.
