/** Technical identifiers that must never be expanded into a public full name. */
export const TECHNICAL_IDENTITY = {
  repositoryName: "jayantgoyal",
  packageScope: "@jayantgoyal",
  primaryDomain: "jayantgoyal.com",
} as const;
