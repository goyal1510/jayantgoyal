# Weather

A single-page weather dashboard built with Next.js and Tailwind.

## Development

From the repo root:

```bash
pnpm dev --filter weather
```

## Environment

Create `.env.local` in `apps/weather` with your OpenWeather API key:

```
NEXT_PUBLIC_OPENWEATHER_API_KEY=your_api_key_here
```
