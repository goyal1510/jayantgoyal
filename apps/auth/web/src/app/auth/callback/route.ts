/**
 * Compatibility handler for product-era email templates that still point to
 * `/auth/callback`. The standalone Auth callback remains `/callback`.
 */
export { GET } from "@/app/callback/route";
