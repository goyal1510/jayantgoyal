# Supabase
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Auth canonical URL (local default; staging/production values stay environment-specific)
NEXT_PUBLIC_SITE_URL=http://localhost:3003

# Enable only on approved localhost/staging hosts until the SSO observation gate passes
PLATFORM_SESSION_ENABLED=false
