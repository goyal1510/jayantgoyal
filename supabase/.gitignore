# Supabase CLI machine-local state
/.branches/
/.temp/

# Local secrets and dotenvx encryption keys
.env
.env.*
!.env.example
!.env.*.example
.env.keys

# Local database exports and backups may contain production data
/backups/
/dumps/
*.backup
*.dump
