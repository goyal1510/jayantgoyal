# Personal Overrides

## Git

- Always commit under my name — never add Co-Authored-By lines.
- Switch to `gh auth switch --user goyal1510` before any GitHub CLI operations.
- Remote uses SSH alias: `git@github-p:goyal1510/jayantgoyal.git`
